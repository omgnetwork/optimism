{
  "name": "dao",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "yarn truffle migrate --network development",
    "migrate:local_l1": "yarn truffle migrate --network local_l1 --config truffle-config.js",
    "migrate:local_l2": "yarn truffle migrate --network local_l2 --config truffle-config-ovm.js",
    "migrate:rinkeby_l2": "yarn truffle migrate --network rinkeby_l2 --config truffle-config-ovm.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@eth-optimism/solc": "0.5.16-alpha.6",
    "@truffle/hdwallet-provider": "^1.4.3",
    "ethers": "^5.4.2",
    "fs": "0.0.1-security",
    "truffle": "^5.4.6"
  },
  "devDependencies": {
    "ganache-cli": "^6.12.2"
  }
}
